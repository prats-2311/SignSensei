# Required for local development to authenticate as a service account
# On Google Cloud Run, this is handled automatically.
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account.json

# (Optional) Port to run the server on, defaults to 8000
PORT=8000
